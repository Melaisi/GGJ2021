using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using UnityEngine.Rendering.PostProcessing;

//MUST IMPORT POST PROCESSING STACK TO WORK EFFECTIVELY! THEN UNCOMMENT ALL POST PROCESSING LINES! :)

public class ProgrammingPostProcessing : MonoBehaviour
{
    /*[SerializeField] private PostProcessVolume activeVolume;

    public void ToggleAO(bool value)
    {
        AmbientOcclusion ao;
        activeVolume.profile.TryGetSettings(out ao);

        if (value)
        {
            ao.active = true;
        }

        else
        {
            ao.active = false;
        }
    }*/
}
